# Copy to .env in the enrichment/ folder before running docker compose.

# Long random string used to sign login tokens
JWT_SECRET=

# First admin account, created automatically on first startup
ADMIN_USERNAME=admin
ADMIN_PASSWORD=

# Backend only (used when running outside Docker):
# DATABASE_URL=postgresql://enrich:enrich@localhost:5433/enrichment
